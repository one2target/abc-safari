# Инвентарь, значения слов, комната и лабиринт Мариуса — 21 сентября 2026

Команды из корня репозитория:

- `node tests/character-renderer.test.cjs`
- `node tests/find-object-engine.test.cjs`
- `node tests/inventory.test.cjs`
- `node tests/letter-maze-engine.test.cjs`
- `node tests/room.test.cjs`
- `node tests/translation-lessons.test.cjs`
- `node tests/course-regression.test.cjs`

Все семь проверок проходят. Character renderer: одиннадцать inventory-совместимых экранных контекстов используют общий stack; проверены обе куртки, bouquet/balloon, замена внутри слота, outfit + hand_right, снятие, reload, порядок слоёв, лабиринт и изоляция сюжетных иллюстраций.

Значения слов: для A–F проверены шесть русских MP3, карточка без видимого русского текста, English → пауза → Russian и repeat, meaning quiz из трёх перемешанных visuals, retry без учебного штрафа, правильная русская запись, mute, отсутствие translation TTS и безопасный сдвиг старых lesson steps.

Find-object engine: две разные конфигурации с разными изображениями, объектами и процентными координатами используют один движок; проверены стабильные ID, изоляция state по `gameId`, counter/remaining, завершение и валидация config.

Инвентарь: четыре существующих предмета в едином каталоге, восемь стандартных слотов, общие операции unlock/equip/unequip, сохранение и reload, отклонение неизвестных предметов, миграция rewardStateVersion 2 (`giraffeVariant` / `handItem`) в `outfit` / `hand_right` без потери позиции урока.

Комната: вход после C, три раунда с любым порядком выбора, защита от повторного клика, мягкая ошибка без учебного штрафа, hint после третьей ошибки, reload до/после завершения раунда, повтор, reset, отсутствие localStorage, загрузка/ошибка изображения. Прежнее поле `roomABC`, старые сохранения до проверки, начатая проверка и последующие уроки мигрируют без потери позиции. Финал комнаты возвращает прежнюю проверку из пяти вопросов; награда выдаётся после неё.

Лабиринт: исходный PNG 941 × 1672 подключён как background asset; проверены 36 процентных клеток, по 12 видимых букв в каждом раунде, отдельный label overlay с PNG-калибровкой `labelOffsetX/labelOffsetY`, targets только на буквах, nearest-letter hit-test, автопроход нескольких пустых клеток одним нажатием, последовательная анимация character stack, блокировка повторного ввода, запрет пропуска следующей правильной буквы, неправильная буква без перемещения, существующие `d_name` / `e_name` / `f_name`, mute, D → E → F, восстановление state и completion markup.

Регрессия: два полных прохода по 33 прежних учебных ответа и 6 meaning quiz, комнате A/B/C, трём раундам лабиринта, двум выборам наград, миграции, reload, двойные нажатия, статистика, 500 наборов вариантов, 40 проверок типов финального теста, звук/паузы/repeat/mute/отмена и родительский доступ.

Геометрия hotspot комнаты проверена расчётно для 1440×900, 1024×768, 768×1024, 390×844, 360×800, 844×390, 320×740 и 280×640. Вертикальная сцена лабиринта расчётно помещается на 430×932, 390×844, 375×667, 360×800, 320×568, 280×640 и 844×390 без обрезки изображения. Это **не браузерный layout-тест**.

Build отсутствует: проект — статические HTML/CSS/JS без package.json. Скрипты проверены Node.js; локальный сервер, страница и все новые MP3 отвечают HTTP 200. Визуальная проверка карточки значения/meaning quiz и пошаговой анимации лабиринта, настоящие клики/Tab/Enter/Space и browser console остаются ручными: Browser не запускается (`trusted Node process exited unexpectedly`) из-за системной ошибки `TIOCSTI`. Проверить через `/play/?demo=1` по инструкции README. Не считать старые визуальные проверки ниже проверкой новых экранов или новой навигации лабиринта.

---

# Проверки ABC Safari

## Текущее обновление · 15 сентября 2026

Автотесты node work/verify-feedback-stickers.cjs и node work/verify-reward-ownership.cjs проходят на текущем коде. Проверены 1000 случайных реакций и 60 экранов завершения A–F, общий выбор без повторов, ошибка → успех, быстрые нажатия, сбои/кеш изображений и отсутствие стикеров в сохранениях. Два полных курса по 38 ответов подтверждают прежние награды и замки, статистику и переходы.

Аудиоменеджер взят без изменения байтов из актуального main 0f3fa3b9c03ce344cd7081e94a8f6fbd6233836d, сохраняя ручные исправления iPhone/Safari. Модель Audio в тестах учитывает load() и естественное завершение записи; проверены очередь, паузы, repeat, mute и отмена. 60 MP3 и все PNG неизменны. Название ABC Safari и общая область персонажа с translate(-3%,0) восстановлены из ручной версии.

Визуальный тест текущей версии и проверка на физическом iPhone не выполнены: инструмент браузера завершается до исполнения команд даже после сброса. Ниже оставлена история проверок от 13 сентября; она не подтверждает вёрстку нового экрана завершения. Статус публикации описан в STICKER-UPDATE.md.

# История: обновление наград · 13 сентября 2026

## Автоматические проверки

Запуск из корня проекта: node work/verify-reward-ownership.cjs. Результат — work/reward-ownership-test-results.json, passed: true.

- Два полных прохождения по 38 ответов: A–C, mini 5, D–F, mini 5, final 10; mastery всех букв 3/3.
- Сценарий A: jacket_stars + accessory_balloon. Сценарий B: jacket_racer + accessory_bouquet. В каждом случае принадлежат только две выбранные вещи.
- Закрытые альтернативы не надеваются через equipItem; второй вызов chooseReward, в том числе сразу после первого, отклоняется.
- Reload до выбора и после выбора, обязательная награда не повторяется, незавершённый выбор сохраняется.
- Старые v1-сохранения и прежняя v2 с обеими unlockedItems: перенос выбранной куртки, закрытие альтернативы, новый выбор аксессуара вместо головного убора. Статистика и позиция совпадают до/после миграции. Повторная миграция не выдаёт вещи.
- Промежуточные сохранения с новыми именами предметов и без нового маркера версии.
- Reset очищает ownedItems, экипировку, claimedRewards и pending flow; mute сохраняется.
- Ошибка localStorage сохраняет работоспособность в памяти; сохранение в середине задания и защита двойного ответа.
- Взвешенные повторения, 500 наборов неповторяющихся вариантов, 40 проверок покрытия типов финальной игры.
- Последовательности аудио с паузами 500 мс, repeat, cancel, mute, отмена во время паузы, один аудиообъект и не более одного одновременно играющего файла. Нормальный сценарий без TTS, корректные языки fallback, отказ autoplay не вызывает TTS.
- Родительский доступ по удержанию 2 секунды; отсутствие картинки не ломает интерфейс.

## Проверки через браузер

Оба сценария пройдены на отдельном demo-сохранении через обычные кнопки заданий, без подмены состояния или обхода mini challenge.

Сценарий A: чистый старт → A–C + mini → reload незавершённой награды → звёздная куртка → reload выбранной награды → домой и в коллекцию → D–F + mini → reload → шарик → reload → финальные 10 вопросов. На главной отображаются background_home, giraffe_jacket_stars и accessory_balloon. Гоночная куртка и букет disabled.

Сценарий B: сброс с подтверждением → базовый жирафик на домашнем фоне, mute сохранён → A–C + mini → гоночная куртка → D–F + mini → букет → reload → финальные 10 вопросов. На главной отображаются background_home, giraffe_jacket_racer и accessory_bouquet. Звёздная куртка и шарик disabled.

На обоих праздниках используется background_reward; на первом карточки содержат отдельные reward_icon_jacket изображения. Повторный выбор закрытого предмета отсутствует и после reload. PNG загружаются, звук 03_new_letter.mp3 проигрывается без ошибки. Консоль без JavaScript ошибок и предупреждений.

## Верстка и неизменность исходников

Проверены 320 × 568, 390 × 844, 844 × 390 и 1280 × 900. Ширина страницы совпадает с viewport; персонаж и аксессуар имеют одинаковые прямоугольники размещения. Главная, обе награды и коллекция проверены визуально. На коротких экранах используется обычная вертикальная прокрутка.

Данные букв, генерация вопросов, mastery, статистика, учебные переходы и основной загрузчик прогресса сравнены с резервной копией и совпадают. Все предоставленные PNG и audio-manager.js проверены по SHA-256 без изменений. Каталог по-прежнему содержит все 60 MP3.

Физический iPhone/iOS Safari недоступен для теста; браузерная проверка размеров его не заменяет.

## Дополнение: Marius, 15 сентября

После изменения обратной связи прежние регрессионные тесты проходят. Отдельные проверки: node work/verify-feedback-stickers.cjs. Проверены случайный выбор без повторов, retry → success, быстрые нажатия, сбои изображений, неизменность аудио/сохранений и фоновая загрузка. На этом этапе визуальная проверка была заблокирована ошибкой запуска инструмента браузера TIOCSTI. Актуальные результаты приведены в начале документа и в STICKER-UPDATE.md.
